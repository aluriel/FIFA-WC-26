[project]
name = "fifa-wc-26"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "requests",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "tools/serve_scores.py:app"
